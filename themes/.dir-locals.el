;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nil (eval (lambda () (when (string= (file-name-extension buffer-file-name) "html") (jinja2-mode))))))
